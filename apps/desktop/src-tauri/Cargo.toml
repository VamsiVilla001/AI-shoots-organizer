[package]
name = "skwad-desktop"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true
description = "SKWAD Media Organiser desktop application"
default-run = "skwad-desktop"

[lib]
# Deliberately not `skwad_desktop`: the bin target of the same name would emit a
# `skwad_desktop.pdb` to the same path, which Cargo warns about today and will
# reject outright in future.
name = "skwad_desktop_lib"
crate-type = ["staticlib", "cdylib", "rlib"]

[build-dependencies]
tauri-build = { version = "2", features = [] }

[dependencies]
skwad-clustering = { path = "../../../crates/clustering" }
skwad-catalogue = { path = "../../../crates/catalogue" }
skwad-database = { path = "../../../crates/database" }
skwad-export-engine = { path = "../../../crates/export-engine" }
skwad-face-detection = { path = "../../../crates/face-detection" }
skwad-face-recognition = { path = "../../../crates/face-recognition" }
skwad-media-core = { path = "../../../crates/media-core" }
skwad-video-analysis = { path = "../../../crates/video-analysis" }

# No `protocol-asset`: media is served through the skwadmedia:// handler, which
# resolves ids against the database instead of exposing raw filesystem paths.
tauri = { version = "2", features = [] }
tauri-plugin-dialog = "2"
tauri-plugin-opener = "2"

anyhow = "1.0"
argon2 = { version = "0.5", features = ["zeroize"] }
thiserror = "2.0"
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"
chrono = "0.4"
base64 = "0.22"
blake3 = "1.8"
image = "0.25"
keyring = { version = "3.6", features = ["windows-native", "apple-native"] }
num_cpus = "1.17"
parking_lot = "0.12"
reqwest = { version = "0.12", default-features = false, features = ["json", "rustls-tls"] }
rpassword = "7.4"
tracing = "0.1"
tracing-subscriber = { version = "0.3", features = ["env-filter"] }
uuid = { version = "1.18", features = ["v4"] }
zeroize = "1.8"

[dev-dependencies]
tempfile = "3.27"

[features]
default = []
# Mirrors the AI crates' provider features so the whole app can be built with
# GPU acceleration in one flag.
directml = ["skwad-face-detection/directml", "skwad-face-recognition/directml"]
coreml = ["skwad-face-detection/coreml", "skwad-face-recognition/coreml"]
cuda = ["skwad-face-detection/cuda", "skwad-face-recognition/cuda"]
opencv-tracking = ["skwad-video-analysis/opencv-tracking"]
